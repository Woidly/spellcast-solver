{
  "name": "automatic",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "build": "bun build client/index.ts --target=browser --banner \"$(cat client/meta.txt)\" --minify --minify-syntax --minify-whitespace --minify-identifiers --outfile=dist/spellcast.userscript.js",
    "server": "bun run server/index.ts"
  }
}
