[package]
name = "spellcast-solver"
version = "0.1.0"
edition = "2021"

[dependencies]
argh = "0.1.12"
getch-rs = { version = "0.2.0", optional = true }
once_cell = "1.20.2"

[features]
interactive = ["getch-rs"]
